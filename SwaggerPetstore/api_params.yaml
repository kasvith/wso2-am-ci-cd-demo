environments:
  - name: dev
    endpoints:
      production:
        url: 'https://dev.wso2.com'
        config:
          retryTimeOut: $RETRY
  - name: test
    endpoints:
      production:
        url: 'https://test.wso2.com'
        config:
          retryTimeOut: $RETRY
  - name: prod
    endpoints:
      production:
        url: 'https://prod.wso2.com'
        config:
          retryTimeOut: $RETRY